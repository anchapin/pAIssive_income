[pytest]
# Use the local conftest.py instead of the global one
confcutdir = tests/ai_models/adapters
# Disable loading of conftest.py from parent directories
noconftest = True
# Disable warnings about missing Flask
filterwarnings =
    ignore::ImportWarning
    ignore::DeprecationWarning
